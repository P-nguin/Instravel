services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: instravel
      POSTGRES_USER: instravel
      POSTGRES_PASSWORD: scout123
    ports:
      - "5435:5432"
    volumes:
      - instravel-postgres:/var/lib/postgresql/data

volumes:
  instravel-postgres: